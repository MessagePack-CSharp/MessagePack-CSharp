<Project>

  <!-- xUnit's style analyzers are noise for this repo (user decision); grow the list as
       new rules start firing. $(NoWarn) accumulation keeps the SDK defaults (1701;1702). -->
  <PropertyGroup>
    <NoWarn>$(NoWarn);xUnit1051;xUnit2013</NoWarn>
  </PropertyGroup>

</Project>
