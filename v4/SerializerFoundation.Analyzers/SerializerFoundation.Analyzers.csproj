<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <LangVersion>latest</LangVersion>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <EnforceExtendedAnalyzerRules>true</EnforceExtendedAnalyzerRules>
    <IsRoslynComponent>true</IsRoslynComponent>
    <Description>Analyzers enforcing SerializerFoundation's buffer contracts: SF002 (buffer structs are single-owner and must not be copied). Foundation-level rules only; serializer-specific analyzers live with their own generator. (SF001, the mandatory-net10.0-target rule, was retired when the serializer gained the runtime compatibility-fallback path.)</Description>
    <!-- RS2008: analyzer release tracking files are packaging ceremony, not needed in the lab -->
    <NoWarn>$(NoWarn);RS2008</NoWarn>
  </PropertyGroup>

  <ItemGroup>
    <!-- keep the floor low: the IDE loads analyzers with ITS OWN Roslyn, so targeting a
         new Microsoft.CodeAnalysis breaks Visual Studio versions older than that Roslyn
         while dotnet build keeps working. Pinned to match UltraMessagePack.SourceGenerator. -->
    <PackageReference Include="Microsoft.CodeAnalysis.CSharp" Version="4.8.0" PrivateAssets="all" />
  </ItemGroup>

</Project>
