<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<!-- net11.0 uses the in-box System.IO.Compression.ZstandardEncoder/Decoder (no package dependency);
		     the other targets have no Zstandard in the BCL and bind NativeCompressions.Zstandard instead. Both wrap
		     libzstd, so the frames are interchangeable and the processor code is shared but for a small shim. -->
		<!-- netstandard2.1 is the floor because NativeCompressions has no netstandard2.0 asset; the core starts at
		     netstandard2.0 and this package follows as soon as NativeCompressions ships that target. -->
		<TargetFrameworks>netstandard2.1;net10.0;net11.0</TargetFrameworks>
		<LangVersion>preview</LangVersion>
		<ImplicitUsings>enable</ImplicitUsings>
		<Nullable>enable</Nullable>
		<AllowUnsafeBlocks>true</AllowUnsafeBlocks>
		<IsAotCompatible Condition="$([MSBuild]::IsTargetFrameworkCompatible('$(TargetFramework)', 'net8.0'))">true</IsAotCompatible>
		<Description>Zstandard compression for MessagePack as a MessagePackMessageProcessor — ext 96 envelope or standard Zstandard frames, composed purely through options. Uses the BCL codec on .NET 11 and NativeCompressions.Zstandard below.</Description>
	</PropertyGroup>

	<!-- The netstandard2.1 asset is never loaded by .NET Framework (which stops at netstandard2.0), so the strong-name
	     mismatch against the unsigned NativeCompressions assemblies (CS8002) cannot bite there; every other runtime ignores it. -->
	<PropertyGroup Condition="'$(TargetFramework)' == 'netstandard2.1'">
		<NoWarn>$(NoWarn);CS8002</NoWarn>
	</PropertyGroup>

	<ItemGroup>
		<Using Include="System.Buffers" />
	</ItemGroup>

	<ItemGroup Condition="'$(TargetFramework)' != 'net11.0'">
		<PackageReference Include="NativeCompressions.Zstandard" Version="0.6.1" />
	</ItemGroup>

	<ItemGroup>
		<ProjectReference Include="..\MessagePack\MessagePack.csproj" />
		<ProjectReference Include="..\SerializerFoundation.Analyzers\SerializerFoundation.Analyzers.csproj" OutputItemType="Analyzer" ReferenceOutputAssembly="false" />
		<ProjectReference Include="..\MessagePack.SourceGenerator\MessagePack.SourceGenerator.csproj" OutputItemType="Analyzer" ReferenceOutputAssembly="false" />
	</ItemGroup>

</Project>
